#include <stdio.h>
#include <string.h>
int main()
{ 
   char a[20], b[12];
    int c;
   scanf("%[^\n]s %[^\n]s",a,b );
   if(strlen(a)==strlen(b)){
    for(int i=0;i<strlen(a);i++){
        for(int j=0;j<strlen(b);j++){
        if(a[i]==b[j])
        {
        c++;
        a[i]='*';
        break;
        }
    }  
   }
   }
   if(c==strlen(a))
   printf("Anagram");
   else
   printf("Not a Anagram");
}
    

