#include <stdio.h>
int main()
{
    int n,o=1,b=2;
    printf("Enter the number to check:");
    scanf("%d",&n);
    for(int i=1;i<=n-1;i++){
        o=o*b;
    }
    
  printf("%d",o);
    return 0;
}
